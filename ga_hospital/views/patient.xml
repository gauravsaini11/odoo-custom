<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_create_appointments" model="ir.actions.server">
        <field name="name">Create Appointment</field>
        <field name="model_id" ref="ga_hospital.model_hospital_patient"/>
        <field name="binding_model_id" ref="ga_hospital.model_hospital_patient"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
            if record:
                action_values = evn.ref('ga_hospital.action_create_appointment').sudo().read()[0]
                action_values.update({'context': evn.context})
                action = action_values
        </field>
    </record>

    <!-- Create Kanban view -->
    <record id="view_patient_kanban" model="ir.ui.view">
            <field name="name">hospital.patient.kanban</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <kanban default_order="id asc">
                    <field name="id"/>
                    <field name="name"/>
                    <field name="age"/>
                    <field name="gender"/>
                    <field name="note"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="oe_kanban_global_click">
                                <div class="row">
                                    <ul>
                                        <li>
                                            Name: <field name="name"/>
                                        </li>
                                        <li>
                                            Age: <field name="age"/>
                                        </li>
                                        <li>
                                            Gender: <field name="gender"/>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
    </record>

    <!-- create tree view -->
    <record id="view_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree expand="1">
                <field name="name"/>
                <field name="responsible_id"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="note"/>
                <field name="appointment_count"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- create form view -->
    <record id="view_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <!-- State bar box -->
                <header>
                    <button id="button_confirm" name="action_confirm" string="Confirm" class="btn-primary" states="draft" confirm="Are you sure that you need to confirm ?" type="object"/>
                    <button id="button_done" name="action_done" string="Mark as Done" class="btn-primary"  states="confirm" confirm="Are you want to Mark as done ?" type="object"/>
                    <button id="button_draft" name="action_draft" string="Set to Draft" class="btn-primary" states="cancel" confirm="Are you make it as a draft ?" type="object"/>
                    <button id="button_cancel" name="action_cancel" string="Cancel" states="draft,done,confirm" confirm="Are you sure that you need to cancel ?" type="object"/>
                    <button id="button_create_appointment" name="%(ga_hospital.action_create_appointment)d" string="Create Appointment" class="btn-primary" type="action"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,done,confirm"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_appointments" type="object" class="oe_stat_button" icon="fa-calendar">
                            <div class="o_stat_info">
                                <field name="appointment_count" class="o_stat_value"/>
                                <span class="o_stat_text">Appointments</span>
                            </div>
                        </button>
                    </div>
                    <field name="image" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <h1>
                            <field name="reference" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="responsible_id"/>
                            <field name="age"/>
                            <field name="appointment_count"/>
                        </group>
                        <group>
                            <field name="gender" invisible="context.get('hide_gender')"/>
                            <field name="note"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Appointments" name="appointment">
                            <field name="appointment_ids" readonly="1">
                                <tree create="0" delete="0" edit="0">
                                    <field name="name"/>
                                    <field name="patient_id"/>
                                    <field name="doctor_id"/>
                                    <field name="age" optional="show"/>
                                    <field name="gender" optional="show"/>
                                    <field name="date_appointment" optional="show"/>
                                    <field name="date_checkup" optional="hide"/>
                                    <field name="note" optional="show"/>
                                    <field name="state" optional="show"/>
                                </tree>
                                <form create="0" edit="0">
                                    <group>
                                        <group>
                                            <field name="patient_id"/>
                                            <field name="age"/>
                                            <field name="gender" readonly="1" force_save="1"/>
                                        </group>
                                        <group>
                                            <field name="doctor_id"/>
                                            <field name="date_appointment"/>
                                            <field name="date_checkup"/>
                                        </group>
                                    </group>
                                </form>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <!-- Chatter box -->
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Search view -->
    <record id="view_patient_search" model="ir.ui.view">
        <field name="name">hospital.patient.search</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <search string="Patients">
                <field name="name"/>
                <field name="note"/>
                <separator/>
                <filter string="Male" name="male" domain="[('gender', '=', 'male')]"/>
                <filter string="Female" name="female" domain="[('gender', '=', 'female')]"/>
                <filter string="Other" name="other" domain="[('gender', '=', 'other')]"/>
                <group expand="0" string="Group By">
                    <filter string="Gender" name="gender" domain="[]" context="{'group_by': 'gender'}"/>
                    <filter string="Patient Name" name="patient_name" domain="[]" context="{'group_by': 'name'}"/>
                </group>

            </search>
        </field>
    </record>

    <!-- Acton Model -->
    <record id="action_hospital" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{'search_default_patient_name':1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create patients!
            </p>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem id="menu_hospital_root"
              name="Hospital"
              sequence="10"/>

    <menuitem id="menu_patient_root"
              name="Patients"
              parent="menu_hospital_root"
              sequence="10"/>
    <menuitem id="hospital_patient"
              name="Patients"
              parent="menu_patient_root"
              action="action_hospital"
              sequence="10"/>
</odoo>